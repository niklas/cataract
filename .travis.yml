language: ruby
rvm:
  - 1.9.3
before_script:
- "psql -c 'create database cataract_test;' -U postgres"
- "sudo apt-get install rtorrent"
- "cp config/database.yml{.travis,}"
- "bundle exec rake db:migrate"
script: 
- "bundle exec rake cucumber:ok"
- "bundle exec rspec"
