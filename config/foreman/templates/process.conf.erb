start on starting <%= app %>-<%= name %>
stop on stopping <%= app %>-<%= name %>
respawn

exec bash -c 'source /usr/local/rvm/scripts/rvm && cd <%= engine.root %>; export RAILS_ENV=production; export PORT=<%= port %>;<% engine.env.each_pair do |var,env| %> export <%= var.upcase %>=<%= shell_quote(env) %>; <% end %> <%= process.command %> >> <%= log %>/<%=name%>-<%=num%>.log 2>&1'
