en:
  activerecord:
    attributes:
      torrent:
        url: "URL"
        search_title:
          # order of hash is persisted
          status: "%{value} torrents"
          terms: "containing '%{value}'"
          directory: 'in "%{value}"'
          page: '(page %{value})'
        content_directory: "Content directory"
      move:
        target_directory: "Directory"
        target_disk: "Disk"
      directory:
        virtual: "do not create on disk"
        subscribed: "subscribed"
        filter: "Filter"
  flash:
    torrents:
      create:
        alert: "Could not create Torrent."
        notice:  "Torrent was successfully created."
    move:
      create:
        notice: "moving %{torrent} to %{target}"
    deletion:
      create:
        notice: "%{torrent} deleted"
  simple_form:
    labels:
      defaults:
        delete_content: "delete content"
  helper:
    eta:
      never: 'when pigs can fly'
