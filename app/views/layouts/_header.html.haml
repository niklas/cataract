%header
  .navbar.navbar-fixed-top
    .navbar-inner
      .container-fluid
        = render 'collapse', target: '.nav-collapse'
        = link_to 'Cataract', root_path, class: 'brand'
        .nav-collapse
          = render 'session'
          = render 'navigation'
- if show_title?
  .page-header
    %h1#title= yield(:title)
= render 'flash'

