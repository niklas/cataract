- title :dashboard

%div.filters.btn-group{'data-toggle' => 'buttons-radio'}
  - Torrent::Search::States.each do |state|
    = link_to state, torrents_path(torrent_search: {status: state}), class: 'btn btn-primary'
