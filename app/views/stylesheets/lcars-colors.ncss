/* Color Schemes based on info from http://www.lcarsdeveloper.com */


body {
  background-color: <%= @default_theme[:base] %>;
  color: <%= @default_theme[:text] %>;
}

<% @lcars_colors.sort {|a,b| a[1][:position] || 0 <=> b[1][:position] || 1 }.each do |name,colors| %>
div.lcars {
  .<%= name %> {
    outline: thin solid <%= colors[:base] %>;
    background-color: <%= colors[:elbows] %>;
    a {
      .show {
        background-color: <%= colors[:base] %>;
        color: <%= colors[:color2] %>;
        border-left: 1em solid <%= colors[:color2] %>;
        border-right: 0.1em solid <%= colors[:color2] %>;
        padding: 0 3px;
        :hover {
          background-color: <%= colors[:color2] %>;
          color: <%= colors[:base] %>;
        }
      }
      .edit {
        background-color: <%= colors[:base] %>;
        color: <%= colors[:change] %>;
        border-left: 1em solid <%= colors[:change] %>;
        border-right: 0.1em solid <%= colors[:change] %>;
        padding: 0 3px;
        :hover {
          background-color: <%= colors[:change] %> !important;
          color: <%= colors[:base] %> !important;
        }
      }
    }
    form {
       input[type="submit"], button[type="submit"] {
         background: <%= colors[:base] %>;
         color: <%= colors[:color1] %>;
         border: none;
         :hover {
           background-color: <%= colors[:color1] %> !important;
           color: <%= colors[:base] %> !important;
           border-color: <%= colors[:color1] %>;
         }
       }
       .create {
          input[type="submit"], button[type="submit"] {
            background: <%= colors[:base] %>;
            color: <%= colors[:primary] %>;
            border-color: <%= colors[:primary] %>;
            :hover {
              background-color: <%= colors[:primary] %> !important;
            }
          }
       }
       .destroy {
          input[type="submit"], button[type="submit"] {
            background: <%= colors[:base] %>;
            color: <%= colors[:warning] %>;
            border-color: <%= colors[:warning] %>;
            :hover {
              background-color: <%= colors[:warning] %> !important;
            }
          }
       }
       .update {
          input[type="submit"], button[type="submit"] {
            background: <%= colors[:base] %>;
            color: <%= colors[:change] %>;
            border-color: <%= colors[:change] %>;
            :hover {
              background-color: <%= colors[:change] %> !important;
            }
          }
       }
    }
    ul.buttons {
      background-color: <%= colors[:base] %>;
      > li {
        background-color: <%= colors[:base] %>;
        > a {
          color: <%= colors[:base] %>;
          background-color: <%= colors[:primary] %>;
          :hover {
            background-color: <%= colors[:text] %>;
          }
          .next, .next_page {
            background-color: <%= colors[:color1] %>;
          }
          .prev, .prev_page {
            background-color: <%= colors[:color2] %>;
          }
        }
        form.create button {
          background-color: <%= colors[:primary] %>;
        }
        form.destroy button {
          background-color: <%= colors[:warning] %>;
        }
        form.update button {
          background-color: <%= colors[:change] %>;
        }
        form button[type="submit"] {
          color: <%= colors[:base] %>;
          width: 100%;
          :hover {
            background-color: <%= colors[:text] %> !important;
          }
        }
        > span {
          color: <%= colors[:base] %>;
          background-color: <%= colors[:elbows] %>;
          .disabled {
            color: <%= colors[:offline] %>;
            background-color: <%= colors[:base] %>;
          }
          .current, .gap {
            color: <%= colors[:color1] %>;
            background-color: <%= colors[:base] %>;
          }
        }
        + li > a {
          background-color: <%= colors[:color1] %>;
        }
        + li + li > a {
          background-color: <%= colors[:color2] %>;
        }
      }
      :after {
        background-color: <%= colors[:elbows] %>;
      }
    }
    > * {
      .top, .bottom {
        .title {
          background-color: <%= colors[:base] %>;
          color: <%= colors[:color1] %>;
          input[type="text"] {
             background-color: <%= colors[:input] %>;
             color: <%= colors[:primary] %>;
             border: none;
          }
        }
      }
      .inner {
        background-color: <%= colors[:base] %>;
      }
      .modal {
         opacity: 0.8;
         background-color: <%= colors[:base] %>;
         > div.message {
            opacity: 0.8;
            z-index: 5;
            background-color: <%= colors[:offline] %>;
            color: <%= colors[:text] %>;
         }
      }
      > div.content {
        a.action {
           color: <%= colors[:base] %>;
           background-color: <%= colors[:color1] %>;
        }
        dl {
          dt {
            color: <%= colors[:color1] %>;
          }
          dd {
          }
        }
        form {
          label + input[type="text"],
          label + input[type="textarea"],
          label + input[type="password"] {
             color: <%= colors[:color1] %>;
             border-color: <%= colors[:color1] %>;
             background-color: <%= colors[:input] %>;
          }
          input[type="submit"], button[type="submit"] {
            padding: 0;
            border-left-width: 1em;
            border-left-style: solid;
            border-right-width: 0.1em;
            border-right-style: solid;
          }
        }
        ul {
          li {
            background-color: <%= colors[:base] %>;
            :hover {
              background-color: <%= colors[:unavailable] %>;
            }
          }
          ul {
            background-color: <%= colors[:color1] %>;
            ul {
              background-color: <%= colors[:color2] %>;
              ul {
                background-color: <%= colors[:primary] %>;
              }
            }
          }
        }
        div.pagination {
          * {
            background-color: <%= colors[:primary] %>;
            color: <%= colors[:base] %>;
          }
          span {
            .disabled {
              color: <%= colors[:offline] %>;
              background-color: <%= colors[:base] %>;
            }
            .current, .gap {
              color: <%= colors[:color1] %>;
              background-color: <%= colors[:base] %>;
            }
          }
          a {
            .next, .next_page {
              background-color: <%= colors[:color1] %>;
            }
            .prev, .prev_page {
              background-color: <%= colors[:color2] %>;
            }
            :hover {
              background-color: <%= colors[:text] %>;
            }
          }
        }
      }
    }
  }
}
<% end %>


div.lcars.busy {
  > img.decoration {
    z-index: 23;
  }
}

