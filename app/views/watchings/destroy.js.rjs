page.select("div.torrents.watchlist .#{dom_id @torrent}").remove()
page.select("div.torrents:not(.watchlist) .#{dom_id @torrent}").replace_with render(:partial => 'torrents/card', :object => @torrent )
page.update_helm :title => @torrent.short_title, 
                 :content => { :partial => '/torrents/details', :object => @torrent },
                 :buttons => actions_for_torrent(@torrent)

