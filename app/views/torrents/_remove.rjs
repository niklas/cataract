# removes the given torrent from the list or table
domid = "torrent_#{remove.id}"
page.visual_effect :fade, domid, :duration => 2
page.delay(2) do
  page[domid].remove 
end
page << render_pending_notifications
