= decorate torrent do |t|
  = content_tag_for :div, torrent, 'transfer' do
    .ui-grid-a
      .ui-block-a
        = t.link_to_content
        %p.down= t.down_rate
        %p.up= t.up_rate
        = render 'transfer/buttons', torrent: torrent
      .ui-block-b
        .progress
          .stretcher
          = t.progress
