page.update_main :content => {:partial => '/torrents/list', :object => @torrents}
page[:label_for_term].replace_html title_for_main
if @term.blank?
  page[:torrent_search].reset
  page[:term].hide();
end
