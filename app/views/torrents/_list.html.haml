%table.torrents.table.table-striped{data: { url: torrents_path(search), 'num-pages' => search.results.num_pages }}
  %thead
    %tr
      %th.status
      %th.title= Torrent.human_attribute_name :title
      %th.location= Torrent.human_attribute_name :location
      %th.actions
  %tbody#torrents
    - search.results.each do |torrent|
      = render 'torrents/item', torrent: torrent
