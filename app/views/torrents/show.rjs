target_domid = "torrent_#{@torrent.id}"
details_domid = "details_for_#{@torrent.id}"

page.replace target_domid, render(:partial => '/torrents/details', :object => @torrent)
page.visual_effect :grow, details_domid, :duration => 1.3, :direction => "'top-left'"
