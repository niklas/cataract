<% torrent = card %>
<% content_tag_for :div, torrent do %>
  <%= progress_image_for torrent %>
  <span class="title"> <%= h torrent.short_title %> </span>
  <%= show_torrent_link torrent, :label => 'Details' %>
<% end %>
