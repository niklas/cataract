target_domid = "torrent_#{@torrent.id}"
details_domid = "details_for_#{@torrent.id}"

page.visual_effect :blind_up, details_domid, :duration => 1.3
page.delay(2) do
  page.replace target_domid, render(:partial => '/torrents/preview', :object => @torrent)
end
