{{#with view.content}}
  <div class="row-fluid">
    <div class="span6">
      <div {{bindAttr class=":progress :progress-success finished::progress-striped downloading:active"}}>
        <div class="bar" {{bindAttr style="progressStyle"}}>
          <div class="percent">{{progress}}%</div>
          {{#if eta}}
          <div class="eta">{{eta}}</div>
          {{/if}}
        </div>
      </div>
    </div>
    <div class="span6">
      <span class="down badge"><i class="icon-arrow-down"></i>{{downRate}}</span>
      <span class="up badge"  ><i class="icon-arrow-up"  ></i>{{upRate}}</span>
      {{#if message}}
        <div class="message">{{message}}</div>
      {{/if}}
    </div>
  </div>
{{/with}}
