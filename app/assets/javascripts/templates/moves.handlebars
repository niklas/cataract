<ul id="queue" class="nav pull-right">
  {{#if length}}
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
      <span class="badge badge-info">{{length}}</span>
    </a>
    <ul class="dropdown-menu">
      {{#each move in controller}}
        <li><a href="#">{{move.title}}</a></li>
      {{/each}}
    </ul>
  </li>
  {{/if}}
</ul>

