ul#queue.nav.pull-right
  unless moves.isEmpty

    li.dropdown
      a.dropdown-toggle href="#" data-toggle="dropdown"
        span.badge.badge.info= moves.length
      ul.dropdown-menu
        each moves as |move|
          li
            a href="#"
              = move.title
