{{#with view.content}}
<h4 class="title">
  {{#linkTo torrent this}}
  {{title}}
  {{/linkTo}}
</h4>

{{#if Cataract.online}}
  {{#if isRunning}}
    {{view Cataract.TransferView contentBinding="transfer"}}
  {{/if}}
{{else}}
  {{Cataract.offlineReason}}
{{/if}}
{{/with}}
