if disks.length
  table.table.table-striped.disks
    thead
      tr
        th Name
        th Directories

    tbody
      each disk in disks
       tr
         td
           disk-link content=disk
         td= disk.directories.length
else
  | no diskzes
